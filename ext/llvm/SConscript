import os

Import('main')

# LLVM 9 install location set with the ENV variable $LLVM_INSTALL
llvm_install = Dir(os.environ['LLVM_INSTALL'])
# Include files for LLVM 9 installation.
llvm_include = llvm_install.Dir('include')
# Library path for LLVM 9 installation.
llvm_libdir = llvm_install.Dir('lib')

main.Append(CPPPATH=[llvm_include])
main.Append(LIBPATH=[llvm_libdir])
main.Append(LIBS=['LLVM-9'])
# main.Append(LIBS=['LLVMXRay'])
# main.Append(LIBS=['LLVMWindowsManifest'])
# main.Append(LIBS=['LLVMTextAPI'])
# main.Append(LIBS=['LLVMTableGen'])
# main.Append(LIBS=['LLVMSymbolize'])
# main.Append(LIBS=['LLVMDebugInfoPDB'])
# main.Append(LIBS=['LLVMOrcJIT'])
# main.Append(LIBS=['LLVMJITLink'])
# main.Append(LIBS=['LLVMObjectYAML'])
# main.Append(LIBS=['LLVMMCA'])
# main.Append(LIBS=['LLVMLTO'])
# main.Append(LIBS=['LLVMPasses'])
# main.Append(LIBS=['LLVMObjCARCOpts'])
# main.Append(LIBS=['LLVMLineEditor'])
# main.Append(LIBS=['LLVMLibDriver'])
# main.Append(LIBS=['LLVMInterpreter'])
# main.Append(LIBS=['LLVMFuzzMutate'])
# main.Append(LIBS=['LLVMMCJIT'])
# main.Append(LIBS=['LLVMExecutionEngine'])
# main.Append(LIBS=['LLVMRuntimeDyld'])
# main.Append(LIBS=['LLVMDlltoolDriver'])
# main.Append(LIBS=['LLVMOption'])
# main.Append(LIBS=['LLVMDebugInfoGSYM'])
# main.Append(LIBS=['LLVMCoverage'])
# main.Append(LIBS=['LLVMCoroutines'])
# main.Append(LIBS=['LLVMXCoreDisassembler'])
# main.Append(LIBS=['LLVMXCoreCodeGen'])
# main.Append(LIBS=['LLVMXCoreDesc'])
# main.Append(LIBS=['LLVMXCoreInfo'])
# main.Append(LIBS=['LLVMMIRParser'])
# main.Append(LIBS=['LLVMipo'])
# main.Append(LIBS=['LLVMInstrumentation'])
# main.Append(LIBS=['LLVMVectorize'])
# main.Append(LIBS=['LLVMLinker'])
# main.Append(LIBS=['LLVMIRReader'])
# main.Append(LIBS=['LLVMAsmParser'])
# main.Append(LIBS=['LLVMGlobalISel'])
# main.Append(LIBS=['LLVMSelectionDAG'])
# main.Append(LIBS=['LLVMAsmPrinter'])
# main.Append(LIBS=['LLVMDebugInfoDWARF'])
# main.Append(LIBS=['LLVMCodeGen'])
# main.Append(LIBS=['LLVMTarget'])
# main.Append(LIBS=['LLVMScalarOpts'])
# main.Append(LIBS=['LLVMInstCombine'])
# main.Append(LIBS=['LLVMAggressiveInstCombine'])
# main.Append(LIBS=['LLVMTransformUtils'])
# main.Append(LIBS=['LLVMBitWriter'])
# main.Append(LIBS=['LLVMAnalysis'])
# main.Append(LIBS=['LLVMProfileData'])
# main.Append(LIBS=['LLVMObject'])
# main.Append(LIBS=['LLVMBitReader'])
# main.Append(LIBS=['LLVMBitstreamReader'])
# main.Append(LIBS=['LLVMCore'])
# main.Append(LIBS=['LLVMRemarks'])
# main.Append(LIBS=['LLVMMCParser'])
# main.Append(LIBS=['LLVMMC'])
# main.Append(LIBS=['LLVMDebugInfoCodeView'])
# main.Append(LIBS=['LLVMDebugInfoMSF'])
# main.Append(LIBS=['LLVMBinaryFormat'])
# main.Append(LIBS=['LLVMSupport'])
# main.Append(LIBS=['LLVMDemangle'])
# -ltinfo needed for llvm
main.Append(LIBS=['tinfo'])